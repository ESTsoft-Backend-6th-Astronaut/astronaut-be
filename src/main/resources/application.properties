spring.application.name=BE

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# H2 Database Configuration

spring.datasource.url=jdbc:h2:file:~/testdb7777
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update

# JPA configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Auto ddl creation configuration
#spring.jpa.generate-ddl=true

# DB Reset configuration (DANGER)
#spring.jpa.generate-ddl=true
#spring.jpa.hibernate.ddl-auto=create
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.defer-datasource-initialization=true
#spring.sql.init.mode=always

# include application-local.properties configuration
spring.profiles.include=local

# Import .env file
spring.config.import=optional:file:.env[.properties]

# Api Key
naver.client.id=${NAVER_API_ID}
naver.client.secret=${NAVER_API_SECRET}
allen.client.id=${ALLEN_API_ID}
krx.client.id=${KRX_API_ID}